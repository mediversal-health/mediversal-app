import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  callingCodeText: {
    color: '#555',
    fontSize: 16,
    fontWeight: '500',
    marginLeft: 4,
  },
  container: {
    alignItems: 'center',
    flexDirection: 'row',
    paddingHorizontal: 8,
    paddingVertical: 4,
  },
});
export default styles;
